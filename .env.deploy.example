# Optional: copy to .env.deploy and fill values.
# If not present, deploy script can use your existing local Wrangler login/token.

CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_PAGES_PROJECT=advui
CLOUDFLARE_PAGES_BRANCH=main

# Optional: change the remote name that triggers deploy in pre-push hook
# CF_DEPLOY_REMOTE=publish
